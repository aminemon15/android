<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.gresikdev.foodbank">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.RegisterActivity" android:theme="@style/tanpaToolbar"></activity>
        <activity android:name=".views.LoginActivity" android:theme="@style/tanpaToolbar" />
        <activity android:name=".views.WelcomeActivity" android:theme="@style/tanpaToolbar" />
        <activity android:name=".views.MainActivity" android:theme="@style/tanpaToolbar"/>
        <activity android:name=".views.SplashActivity" android:theme="@style/tanpaToolbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>